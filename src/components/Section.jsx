import React from 'react';
import Content from './Content';

const Section = ({ theme }) => {
    return (
        <div>
            <h1>This is a section</h1>
            <Content/>
        </div>
    );
};

export default Section;